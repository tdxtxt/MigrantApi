<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soecode.ton.dao.CityDao">
	<select id="queryAll" resultType="com.soecode.ton.entity.City">
		<!-- 具体的sql -->
		SELECT
		*
		FROM
		system_acer_code
	</select>
</mapper>